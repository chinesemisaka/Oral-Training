# N04 验证记录

日期：2026-09-21。基于已验证的 N03 继续，未合并上游。

两种模式共用已发布服务选择入口，按服务过滤兼容场景、进行中会话和客服训练最佳成绩。空目录、无兼容场景、网络/登录失败可刷新，旧版通用训练需要显式切换。服务会话创建意图按用户、模式、服务、场景持久化；网络失败、鉴权刷新、页面恢复保持 clientSessionId，重复点击受互斥保护。

客服聊天展示初始化排队/生成中/失败/就绪，失败可显式重试；就绪前禁用输入、提示、结束。v2 仅显示公开患者画像及情绪，不展示信任度、私有画像或证据答案。hide/unload 清理轮询并作废响应，onShow 重新读取已保存会话；旧版会话保留原行为。历史列表显示服务名称，归档后已有会话仍可按 sessionId 恢复。

本地 client_recovery_test、patient_client_test 和 page_render_check（51 项）通过。新测试覆盖鉴权重放不带自定义画像、持久意图、模式/服务/用户隔离、重复创建、迟到响应、初始化操作门控、失败重试、续练及旧版兼容。数据库测试补充同场景双服务续练隔离、兼容场景过滤和归档后恢复。

完整 [Windows/PostgreSQL CI](https://github.com/chinesemisaka/Oral-Training/actions/runs/35613315996) 已通过，验证提交 `4e36d9ca60c07c305b7974cae1e36c8dbd7f4d02`（2026-09-22 确认结果）：

- MSVC Release 构建成功；CTest 10 项通过，2 项数据库测试按设计跳过并在后续独立步骤执行通过。
- 52 个 JavaScript、37 个 JSON 文件静态检查，以及客户端恢复、患者初始化交互测试通过。
- 空库、历史数据与重复迁移，知识存储/管理 API、患者初始化及服务隔离数据库测试通过。
- 数据库功能、无模型 API、双模式状态机和每模式 20 个请求的并发测试通过。

未调用真实模型，当前环境无法运行微信开发者工具/真机；WXML 视觉与真机生命周期需人工验收。训练报告的证据核验展示留待 N05/N06，本阶段不在聊天中提前开放答案。
